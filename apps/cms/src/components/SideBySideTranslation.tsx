'use client';

import React from 'react';

export const SideBySideTranslation = () => (
  <div style={{ fontSize: '12px', padding: '8px', border: '1px dashed #999' }}>
    Use Locale tabs + Local API compare endpoint for side-by-side translation review.
  </div>
);

export default SideBySideTranslation;
